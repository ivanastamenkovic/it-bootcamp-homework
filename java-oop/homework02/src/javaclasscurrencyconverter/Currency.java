package javaclasscurrencyconverter;

// Currency is an enum, while RSD is an integer. Currency should be one of:
// EUR, USD, AUD, CAD, RUB, and GBP.

public enum Currency {
    EUR, USD, AUD, CAD, RUB, GBP
}
